<h1>Atlas</h1>

<h2>Continents</h2>

<ul>
  <% @continents.each do |continent|%>
    <li>
      <%= link_to continent.name, continent_path(continent.id)%>
      <%= link_to "Edit", edit_continent_path(continent.id)%>
    </li>
  <% end %>
</ul>
